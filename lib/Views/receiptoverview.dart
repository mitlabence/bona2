import 'package:flutter/material.dart';

class ReceiptOverView extends StatelessWidget {
  const ReceiptOverView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
